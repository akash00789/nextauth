/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    swcMinify: true, // Ensure minification
    // Add any other production-specific configurations here
};

export default nextConfig;



  